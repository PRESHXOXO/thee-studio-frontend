name: Temporary Generation Callsite Trace

on:
  push:
    branches:
      - ops/cast-upload-debug-20260908
  workflow_dispatch:

permissions:
  contents: read

jobs:
  inspect:
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    env:
      CPANEL_SSH_HOST: ${{ secrets.CPANEL_SSH_HOST }}
      CPANEL_SSH_PORT: ${{ secrets.CPANEL_SSH_PORT }}
      CPANEL_SSH_USER: ${{ secrets.CPANEL_SSH_USER }}
      CPANEL_DEPLOY_PATH: ${{ secrets.CPANEL_DEPLOY_PATH }}
      ASSET: assets/index-DhbQbUSO-genfix3-20260908.js

    steps:
      - name: Configure scoped SSH
        env:
          KEY: ${{ secrets.CPANEL_SSH_PRIVATE_KEY }}
          HOSTS: ${{ secrets.CPANEL_SSH_KNOWN_HOSTS }}
        run: |
          install -d -m 700 ~/.ssh
          printf '%s\n' "$KEY" > ~/.ssh/thee_operator
          printf '%s\n' "$HOSTS" > ~/.ssh/known_hosts
          chmod 600 ~/.ssh/thee_operator ~/.ssh/known_hosts

      - name: Print every generation callsite read-only
        run: |
          ssh -o BatchMode=yes -i ~/.ssh/thee_operator -p "$CPANEL_SSH_PORT" "$CPANEL_SSH_USER@$CPANEL_SSH_HOST" \
            "WEB='$CPANEL_DEPLOY_PATH' ASSET='$ASSET' bash -s" <<'REMOTE'
          set -euo pipefail
          python3 - "$WEB/$ASSET" <<'PY'
          import sys
          text=open(sys.argv[1],encoding='utf-8',errors='ignore').read()
          needles=['hL("/generation"','tt("/generation"','`/generation/','"/generation"']
          seen=set()
          for needle in needles:
              start=0
              while True:
                  pos=text.find(needle,start)
                  if pos<0: break
                  if pos not in seen:
                      seen.add(pos)
                      print(f'=== HIT {pos} needle={needle} ===')
                      print(text[max(0,pos-2500):min(len(text),pos+6500)])
                      print('\n---\n')
                  start=pos+len(needle)
          print(f'TOTAL_UNIQUE_HITS={len(seen)}')
          PY
          REMOTE
