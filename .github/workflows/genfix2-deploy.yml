name: Archived One-Time Genfix3 Deploy

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-24.04
    steps:
      - run: echo "Genfix3 was deployed and this one-time workflow is archived."
