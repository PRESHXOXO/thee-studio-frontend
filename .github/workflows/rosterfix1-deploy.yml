name: Archived Cast Roster Rehydration Fix

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-24.04
    steps:
      - run: echo "Cast roster rehydration fix is deployed and this one-time workflow is archived."
